# Application
NODE_ENV=development
PORT=3000

# PostgreSQL
DB_HOST=localhost
DB_PORT=5433   # Docker maps to 5433 to avoid conflict with any local system Postgres on 5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=local_delivery

# TypeORM
DB_SYNCHRONIZE=false
DB_LOGGING=true
